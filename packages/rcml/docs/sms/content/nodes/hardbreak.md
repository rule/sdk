# `hardbreak` (removed)

The `hardbreak` node has been removed in the flat content model. Line breaks
are now embedded directly as `\n` characters within [`message`](./message)
node text — there is no separate break node.
