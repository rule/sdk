# `paragraph` (removed)

The `paragraph` block node has been removed in the flat content model. The
`sms` root node now holds a flat sequence of [`message`](./message),
[`link`](./link), and [`placeholder`](./placeholder) nodes directly — there
are no block wrappers.
