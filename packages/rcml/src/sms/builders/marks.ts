/**
 * @deprecated SMS links are now represented as `SmsLinkNode` top-level nodes,
 * not as marks on text nodes. Use {@link createLinkNode} from `./nodes.ts` instead.
 *
 * This file is kept as an empty barrel to avoid breaking any existing imports
 * while the codebase migrates. It will be removed in a future release.
 *
 * @internal
 */
